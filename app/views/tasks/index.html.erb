<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <div>
    <% completed = task.completed ? "[x]" : "[ ]" %>
    <%= completed  %>
    <%= link_to task.title, task_path(task) %>
  </div>
<% end %>
<%= link_to "Add a new task", tasks_new_path %>
